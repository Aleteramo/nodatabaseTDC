export type MessageKeys = {
  HomePage: {
    title: string;
    subtitle: string;
  };
  Navigation: {
    archive: string;
    about: string;
    contact: string;
  };
  // Add other top-level keys from your JSON files
}
